name: nav_stack
description: A simple but powerful path-based routing system. Based on MaterialApp.router (Nav 2.0) it has full web-browser and deeplink support.
version: 0.0.3
homepage: https://github.com/gskinnerTeam/flutter_nav_stack

environment:
  sdk: ">=2.12.0 <3.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter

  path_stack: ^0.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
